# Draw 対応データモデル設計メモ（完了）

## ステータス

- フェーズ7-Aで設計どおりに実装済み。`Outcome` は `"victory" / "defeat" / "draw"` をサポートし、`CounterState` と HTTP API に `draws` を追加済み。
- 詳細仕様は `docs/specs/api.md` および UI 実装に反映済み。本ドキュメントはアーカイブとして保持する。

## 決定事項の要約

- イベントログと HTTP API は後方互換を維持したまま draw を扱う。
- 管理 UI・CLI・テストは新しいフィールド構成に追従済み。
- 追加アクションは不要で、今後の変更は TODO リストで管理する。
