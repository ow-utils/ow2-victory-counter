[project]
name = "win-detector"
version = "0.0.0"
description = "Overwatch 2 OBS win detector prototype"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
