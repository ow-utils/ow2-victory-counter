const root = document.getElementById("app");

if (root) {
  root.textContent = "win-counter-overlay-ui placeholder";
} else {
  // eslint-disable-next-line no-console
  console.warn("Root element #app が見つかりません。");
}
