過去、 data ディレクトリー以下の画像ファイル(.png)もバージョン管理を行っていました。
git lfs で管理し、GitHubにもpushしているはずです。

それらのファイルについて、履歴から削除し、GitHub側のストレージからも削除したいです。

本操作を行った後の期待する結果は:

- git log から.pngファイルを追加した履歴が消える
- GitHub の LFS用ストレージの利用済み領域が減少する
